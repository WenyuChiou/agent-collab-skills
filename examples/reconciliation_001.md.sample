# Reconciliation Report: Auth Provider Refactor

**Goal:** Refactor `src/auth/` from monolithic `legacy.py` into a
plugin-based provider architecture while preserving stable entry points.

**Timestamp (UTC):** 2026-04-28T10:30:00Z

**Task Count by Agent:**
- claude: 1
- codex: 2
- gemini: 1

**Context policy:** summaries <= 250 words; raw logs path-only.

---

## Per-task summary

### T1 - claude - define-auth-contract
- Status: success
- Output: in-conversation contract decision
- Scope: provider interface, registry shape, legacy compatibility
- Risks: none blocking

### T2 - codex - scaffold-provider-core
- Status: success
- Output: `.ai/codex_result_001_scaffold-provider-core.md`
- Files: `src/auth/interfaces.py`, `src/auth/registry.py`,
  `src/auth/__init__.py`, `src/auth/legacy.py`,
  `tests/auth/test_registry.py`
- Tests: `python -m pytest tests/auth/test_registry.py` passed;
  `python -m mypy src/auth/interfaces.py src/auth/registry.py` passed
- Risks: compatibility imports must remain stable through T4

### T3 - gemini - audit-legacy-surface
- Status: success
- Output: `.ai/gemini_result_001_audit-legacy-surface.md`
- Findings: constructor signatures and exception behavior are the
  migration-sensitive surfaces; direct `legacy.py` dependencies are
  limited to auth tests and compatibility imports
- Risks: provider split must preserve exception types

### T4 - codex - split-providers-and-shims
- Status: fallback
- Output: `.ai/codex_result_001_split-providers-and-shims.md`
- Files: `src/auth/providers/*`, `src/auth/legacy.py`,
  `tests/auth/test_legacy_compat.py`, `tests/auth/test_providers.py`
- Tests: `python -m pytest tests/auth/test_providers.py tests/auth/test_legacy_compat.py`
  failed on one legacy constructor assertion
- Risks: high - compatibility shim needs retry

---

## Cross-task analysis

### Agreement
- T1's contract matches T2's registry scaffold.
- T3's compatibility audit identifies the same legacy surfaces T4
  attempted to preserve.
- No task reports a need to change files outside its declared scope.

### Conflicts
- T4 conflicts with the T1 compatibility rule: one legacy constructor
  assertion fails.
- No file-level conflict between parallel tasks was detected.

### Aggregated risks
1. High: T4 did not preserve one legacy constructor path.
2. Medium: provider exception behavior should be rechecked after retry.

## Recommended action

Retry T4 with an explicit constraint to preserve the failing legacy
constructor. Do not merge the provider split until
`tests/auth/test_legacy_compat.py` passes. T1, T2, and T3 are usable as
inputs for the retry.
